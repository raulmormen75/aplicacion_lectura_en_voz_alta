NEXTAUTH_URL=http://localhost:3000
NEXTAUTH_SECRET=replace_with_a_secure_random_value

# Opcional: endpoint propio en servidor para reconstrucción con IA ligera.
# Si queda vacío, la app usa solo limpieza local sin inventar contenido.
GPT_OSS_ENDPOINT=
